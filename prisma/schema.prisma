generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Usuario {
  id           String   @id @default(uuid())
  name         String
  email        String   @unique
  password     String
  refreshToken RefreshToken?
  isBlocked    Boolean? @default(false)
  dtIniPremium DateTime?
  dtFimPremium DateTime?
  stripeCliId  String?  @unique
  stripeSubId  String?  @unique
  statusAss    String   @default("inativo") // "aberta?","ativo", "cancelado", "pendente"
  plano        String?  // "mensal", "anual", "semestral"
  updatedAt    DateTime @updatedAt
  createdAt    DateTime @default(now())
  @@index([stripeCliId])
}

model RefreshToken {
  id            Int      @id @default(autoincrement())
  expiresIn     Int
  user          Usuario  @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate:   Cascade)
  userId        String   @unique
  updatedAt     DateTime @updatedAt
  createdAt     DateTime @default(now())
}

model Conteudo {
  id             Int      @id @default(autoincrement())
  nome           String?  @unique
  name           String?  @unique
  link           String?  //link do gif
  fonte          String? 
  linkext        String?  //video para redirecionar
  filename       String
  mimetype       String
  data           Bytes
  updatedAt      DateTime @updatedAt
  createdAt      DateTime @default(now())
  categorias     Categoria[]
}
 // @@index([nome])

model Categoria {
  id             Int      @id @default(autoincrement())
  nome           String?  @unique
  name           String?  @unique
  descricao      String?
  updatedAt      DateTime @updatedAt
  createdAt      DateTime @default(now())
  conteudos      Conteudo[]
}